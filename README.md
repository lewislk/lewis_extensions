# lewis_extensions

lewis's entensions

## Build

```bash
pnpm run build:dist
```

The compiled extension is written to `dist/`. In Raycast, use **Import Extension** and select this project/output when importing locally.
